# SQL safety validation logic
# Based on sqlglot to prevent unauthorized table access and non-read-only operations.

# In production, the DB user should also have read-only permissions at the database level.
